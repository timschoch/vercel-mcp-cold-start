# Writing standards

Good defaults, not law — the developer's word overrides anything here.

Run the `unslop` skill on every output before it ships — prose, docs, commit messages, PR bodies, issue text. No exceptions for "small" outputs; slop compounds.

When a correction repeats, encode it as one bad and one good example next to the rule, not as more prose. Agents learn the pair better than the sentence.
